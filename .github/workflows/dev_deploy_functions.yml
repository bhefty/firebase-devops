name: Deploy Firebase Functions

on:
  pull_request:
    branches:
      - release/dev
    paths:
      - "server/firebase/**"
      - dev_deploy_functions.yml
      - "!**.md"
      - "!.gitignore"
  push:
    branches:
      - release/dev
    paths:
      - "server/firebase/**"
      - dev_deploy_functions.yml
      - "!**.md"
      - "!.gitignore"

jobs:
  build:
    name: Deploy Dev
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: Get changed files
        run: echo "::set-env name=FUNCTIONS_TO_DEPLOY::$(./.github/scripts/deploy_changed_functions.sh)"
        env:
          SOURCE_BRANCH: release/dev
          CURRENT_COMMIT: ${{ secrets.GITHUB_SHA }}

      - name: Check if any functions need deployed
        run: echo ::set-output name=DEPLOY_REQUIRED:$(./.github/scripts/does_function_deploy_exist.sh)"

      - name: Setup Node
        if: steps.id.outputs.DEPLOY_REQUIRED == 'true'
        uses: actions/setup-node@v1
        with:
          node-version: "10.x"

      - name: Install Dependencies
        if: steps.id.outputs.DEPLOY_REQUIRED == 'true'
        run: |
          cd server/firebase
          npm ci

      - name: Build
        if: steps.id.outputs.DEPLOY_REQUIRED == 'true'
        run: |
          cd server/firebase
          npm run funcs:clean
          npm run funcs:build

      - name: Deploy
        if: steps.id.outputs.DEPLOY_REQUIRED == 'true'
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only $FUNCTIONS_TO_DEPLOY
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          PROJECT_PATH: "server/firebase"
